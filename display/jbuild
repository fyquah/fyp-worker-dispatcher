(jbuild_version 1)

(executables
 ((names (main))
  (preprocess (pps (ppx_jane  ppx_driver.runner)))
  (libraries
    (; jane street libs
     core
     core_extended
     async
     async_shell

     ; project libs
     protocol

     ; external libs
     gsl
     ))))

(install
 ((section bin)
  (files
    ((main.exe as render)))))
