(jbuild_version 1)

(executables
 ((names (main selection_and_mutate))
  (preprocess (pps (ppx_jane  ppx_driver.runner)))
  (libraries
    (core
     core_extended
     async
     async_shell

     protocol
     fyp_compiler_lib

     owl
     ))))

(install
 ((section bin)
  (files
    ((selection_and_mutate.exe as selection_and_mutate)
     (main.exe as controller)))))
