(jbuild_version 1)

(executables
 ((names (main))
  (preprocess (pps (ppx_jane  ppx_driver.runner)))
  (libraries
    (core
     core_extended
     async
     async_shell

     protocol
     fyp_compiler_lib

     owl
     ))))

(install
 ((section bin)
  (files
    ((main.exe as controller)))))
