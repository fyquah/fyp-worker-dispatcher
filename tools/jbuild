(jbuild_version 1)

(executables
 ((names (now tree_tools data_cleaner local_reward_model))
  (preprocess (pps (ppx_jane  ppx_driver.runner)))
  (libraries
    (core
     async
     async_shell
   
     fyp_compiler_lib
     protocol

     owl
    ))
  ; (ocamlopt_flags
  ;   ((:standard -ccopt "-llapack -lopenblas")))
  ))

(install
 ((section bin)
  (files
    ((now.exe as ocamlnow)
     (tree_tools.exe as tree_tools)
     (data_cleaner.exe as data_cleaner)
     (local_reward_model.exe as local_reward_model)))))
