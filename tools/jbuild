(jbuild_version 1)

(executables
 ((names (now tree_tools data_cleaner))
  (preprocess (pps (ppx_jane  ppx_driver.runner)))
  (libraries
    (core
     async
     async_shell
   
     fyp_compiler_lib
     protocol
    ))))

(install
 ((section bin)
  (files
    ((now.exe as ocamlnow)
     (tree_tools.exe as tree_tools)
     (data_cleaner.exe as data_cleaner)))))
